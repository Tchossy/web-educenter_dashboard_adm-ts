export type OptionType = {
  value: string
  label: string
}

export type ExamQuestionOptionType = {
  text: string
  is_valid: boolean
}
export type TaskQuestionOptionType = {
  text: string
  is_valid: boolean
}
